
runtime: python311
instance_class: F1
handlers:
- url: /.*
  script: auto

service: uat
env_variables:
  IS_PRODUCTION: "false"
  IS_UAT: "true"
  GRAPH_VERSION: "v20"

entrypoint: gunicorn -b :8080 -w 3 main:app --timeout 90

automatic_scaling:
  max_instances: 1